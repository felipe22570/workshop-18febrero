import React from "react";
import { ContBanner } from "../styles/bannerStyle";

const Banner = () => {
   return <ContBanner>¡Adquiere todos tus productos favoritos al mejor precio!</ContBanner>;
};

export default Banner;
